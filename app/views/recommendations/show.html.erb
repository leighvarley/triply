<div class="center jumbotron">
<h2><%= @recommendation.recommended_place %></h2>
</div>

<div>
<p>User: <%= @recommendation.user.name %></p>
<p><%= @recommendation.body %></p>
</div>


<p><%= link_to "Edit", edit_location_recommendation_path(@location, @recommendation), class: "btn btn btn-primary" %></p>

<p><%= link_to "Delete", location_recommendation_path(@location, @recommendation), method: :delete, class: "btn btn btn-primary", data: {confirm: "Are you sure?"} %></p>

<p><%= link_to "Back", :back, class: "btn btn btn-primary" %></p>
