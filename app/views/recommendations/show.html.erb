<h2><%= @recommendation.recommended_place %></h2>

<p><%= @recommendation.body %><p>

<p><%= link_to "Edit", edit_location_recommendation_path(@location, @recommendation) %></p>

<p><%= link_to "Delete", location_recommendation_path(@location, @recommendation), method: :delete %></p>
